const main=document.querySelector('main');let step=0,answers={priorities:[]};
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function explain(key){const e=EXPLAIN[key];return `<details class="explanation"><summary>${e[0]}</summary><p>${e[1]}</p><details><summary>Learn more</summary><p>${e[2]}</p></details></details>`;}
function labCard(l,i){return `<article class="lab-card"><div class="lab-art art-${i}"><span class="art-grid"></span><span class="lab-symbol">${l.icon}</span><span class="art-dot dot1"></span><span class="art-dot dot2"></span><span class="art-caption">${['MAKE THE CONNECTION','EXPLORE THE INVISIBLE','MAKE EVERY CHOICE COUNT'][i]}</span></div><div class="lab-body"><div class="eyebrow">LAB 0${i+1} ${l.soon?'<span class="badge muted">Coming soon</span>':'<span class="badge">Ready to explore</span>'}</div><h3>${l.name}</h3><p>${l.description}</p><div class="meta"><span>◷ ${l.time}</span><span>▥ ${l.level}</span></div><a class="lab-link" href="#${l.id}">Start Lab <span>↗</span></a></div></article>`;}
function home(){return `<section class="hero"><div><div class="eyebrow"><span class="live-dot"></span> A LEARN TO CONNECT INITIATIVE</div><h1>Big possibilities.<br>Better <em>connections.</em></h1><p>Connectivity starts with understanding.<br>Explore hands-on labs that turn complex ideas into practical knowledge for your community.</p><a class="button" href="#builder">Let’s connect a community <span>↗</span></a><span class="hero-note">Free to explore. Built for the curious.</span></div><div class="network-art" role="img" aria-label="A connected community: Internet, tower, school and local users"><svg viewBox="0 0 480 340" aria-hidden="true"><defs><pattern id="dots" width="22" height="22" patternUnits="userSpaceOnUse"><circle cx="1" cy="1" r="1" fill="#c6d6cd"/></pattern></defs><rect width="480" height="340" fill="url(#dots)"/><circle cx="243" cy="175" r="134" fill="none" stroke="#d6e3d8"/><circle cx="243" cy="175" r="90" fill="none" stroke="#d6e3d8"/><g fill="none" stroke="#68a28d" stroke-width="2" stroke-dasharray="5 6"><path d="M90 92L238 170L389 91M238 170L118 283M238 170L381 281"/></g><g fill="#fff" stroke="#d3e0d5"><rect x="39" y="43" width="105" height="86" rx="16"/><rect x="336" y="43" width="105" height="86" rx="16"/><rect x="66" y="243" width="105" height="80" rx="16"/><rect x="328" y="243" width="105" height="80" rx="16"/></g><rect x="177" y="123" width="128" height="105" rx="21" fill="#185e50"/><g font-family="Segoe UI,sans-serif" text-anchor="middle"><g font-size="32" fill="#175e50"><text x="91" y="85">◎</text><text x="389" y="85">⌁</text><text x="118" y="279">⌂</text><text x="381" y="279">♧</text></g><g fill="#51665e" font-size="11"><text x="91" y="111">INTERNET</text><text x="389" y="111">TOWER</text><text x="118" y="303">SCHOOL</text><text x="381" y="303">COMMUNITY</text></g><text x="241" y="178" fill="#d7efb4" font-size="43">⌘</text><text x="241" y="207" fill="white" font-size="11">POSSIBILITY, CONNECTED</text></g></svg><div class="floating-label"><span class="live-dot"></span> One connection. Many opportunities.</div></div></section><section class="labs-section" id="labs"><div class="section-title"><div><div class="eyebrow">YOUR EXPLORATION STARTS HERE</div><h2>Small labs. Real-world understanding.</h2></div><p>No engineering background needed.<br>Just a little curiosity.</p></div><div class="lab-grid">${LABS.map(labCard).join('')}</div></section><section class="bottom-strip"><span class="strip-icon">✳</span><div><h3>Understand the why. Make a better decision.</h3><p>Explore possibilities, see the trade-offs, and build confidence — one connection at a time.</p></div><a href="#learn">Explore the concepts ↗</a></section>`;}
function valid(){return STEPS[step].fields.every(f=>f.multi?answers[f.key]?.length>0:!!answers[f.key]);}
function question(){const s=STEPS[step];return `<div class="workspace"><a class="back-link" href="#labs">← All labs</a><div class="eyebrow">LAB 01 · CONNECTIVITY SOLUTION BUILDER</div><div class="builder-heading"><h1>Let’s connect a community</h1><p>Tell us a little about the location you want to connect. We’ll explore which technologies could work and why.</p></div><div class="progress-label"><span>Step ${step+1} of 6</span><span>${['Your location','Your people','Infrastructure','Landscape','Power','Priorities'][step]}</span></div><div class="progress" role="progressbar" aria-label="Lab progress" aria-valuemin="0" aria-valuemax="6" aria-valuenow="${step+1}">${STEPS.map((_,i)=>`<span class="${i<=step?'done':''}"></span>`).join('')}</div><section class="question-card"><h2>${s.title}</h2><p>${s.hint}</p>${s.why?explain(s.why):''}${s.fields.map(f=>`<fieldset><legend>${f.label||s.title}</legend>${f.why?explain(f.why):''}<div class="options">${f.options.map((o,i)=>{const selected=f.multi?answers[f.key]?.includes(o):answers[f.key]===o;return `<label class="option ${selected?'selected':''}"><input type="${f.multi?'checkbox':'radio'}" name="${f.key}" value="${esc(o)}" ${selected?'checked':''}><span>${o}</span><span class="check" aria-hidden="true">${selected?'✓':'○'}</span></label>`;}).join('')}</div></fieldset>`).join('')}<p id="selection-note" aria-live="polite">${step===5?`${answers.priorities.length} of 2 priorities selected`:''}</p><div class="question-actions"><button class="secondary" id="previous" ${step===0?'disabled':''}>← Back</button><button id="next" ${valid()?'':'disabled'}>${step===5?'Explore my options':'Continue'} →</button></div></section><p class="privacy-note">Your answers stay in this page. No personal data or sign-up needed.</p></div>`;}
function architecture(t){return `<div class="architecture">${t.path.map((p,i)=>`<div class="arch-node"><span class="node-icon">${['◎','⌁','⌘','⋈','♧'][i]}</span><small>${['UPSTREAM','BACKHAUL','SITE CONNECTION','LOCAL DISTRIBUTION','COMMUNITY'][i]}</small><b>${p}</b></div>${i<4?'<span class="connector" aria-hidden="true">→</span>':''}`).join('')}</div>`;}
function results(){if(!STEPS.every(s=>s.fields.every(f=>f.multi?answers[f.key]?.length:answers[f.key])))return question();const r=recommend(answers),t=TECH[r.primary];return `<div class="results"><a href="#builder" class="back-link">← Edit your scenario</a><div class="eyebrow">LAB 01 · YOUR EXPLORATION</div><h1>A connection with possibilities.</h1><p>${esc(answers.site)} · ${esc(answers.users)} users · ${esc(answers.terrain)}</p><section class="recommendation"><div class="eyebrow">RECOMMENDED APPROACH · ${r.rating(r.primary).toUpperCase()}</div><h2>${t.name}</h2><p>A starting point to investigate, based on your answers.</p><h3>Good fit for this scenario because</h3><ul>${r.reasons.map(x=>`<li>${x}</li>`).join('')}</ul><p><b>Your priorities:</b> ${answers.priorities.join(' · ')}. These influence the comparison; local quotes and surveys may change the choice.</p></section><section class="panel"><h2>Possible network architecture</h2><p>Different technologies work together to bring a connection all the way to people.</p>${architecture(t)}${explain('backhaul')}<p class="note">Illustrative path: upstream infrastructure must be confirmed. A second independent link can provide backup; local Wi-Fi does not replace Internet backhaul.</p></section><section class="panel"><h2>Compare the possibilities</h2><p>Qualitative comparisons, not precise engineering calculations. Costs vary by provider, location and service.</p><div class="table-wrap"><table><thead><tr><th>Technology</th><th>Suitability</th><th>Deployment speed</th><th>Capacity</th><th>Cost</th><th>Key trade-off</th></tr></thead><tbody>${r.ranked.map(k=>`<tr ${k===r.primary?'class="primary-row"':''}><th>${TECH[k].name}</th><td><span class="badge">${r.rating(k)}</span></td><td>${TECH[k].speed}</td><td>${TECH[k].capacity}</td><td>${TECH[k].cost}</td><td>${TECH[k].cons[0]}</td></tr>`).join('')}</tbody></table></div><details class="explanation"><summary>Why this result? See the decision rules</summary><p>Rules add qualitative weights. Strong coverage is required to lead with FWA; unknown fiber or unknown tower/terrain cannot lead with those options. Ties follow Fiber, FWA, Microwave, LEO order. Priorities cannot override these gates. These weights are learning assumptions, not measured performance.</p><ul>${r.trace.map(x=>`<li>${x}</li>`).join('')}</ul><p>${Object.entries(r.scores).map(([k,v])=>`${TECH[k].name}: ${v}`).join(' · ')}</p></details><div class="trade-grid">${Object.values(TECH).map(t=>`<details class="technology-card"><summary>${t.name}</summary><ul>${t.pros.map(x=>`<li>+ ${x}</li>`).join('')}${t.cons.map(x=>`<li>− ${x}</li>`).join('')}</ul></details>`).join('')}</div></section><section class="panel"><h2>Before a real deployment</h2><ul>${r.checks.map(x=>`<li>${x}</li>`).join('')}</ul><p class="disclaimer">This is an educational recommendation, not a detailed network design. A real deployment requires site surveys, capacity analysis, spectrum/regulatory checks and detailed technical planning.</p></section><section class="learning"><div class="eyebrow">TAKE THE KNOWLEDGE WITH YOU</div><h2>What did you learn?</h2><div class="lesson-grid">${r.lessons.map(([h,p],i)=>`<article><span>0${i+1}</span><h3>${h}</h3><p>${p}</p></article>`).join('')}</div><button id="reset">Try another scenario ↗</button></section></div>`;}
function render(){const route=location.hash.slice(1)||'home';document.querySelectorAll('nav a').forEach(a=>{if(a.hash===location.hash||(route==='home'&&a.hash==='#home'))a.setAttribute('aria-current','page');else a.removeAttribute('aria-current');});if(route==='builder')main.innerHTML=question();else if(route==='results')main.innerHTML=results();else if(route==='learn')main.innerHTML=`<section class="text-page"><div class="eyebrow">THE LEARNING LIBRARY</div><h1>A little knowledge.<br>A stronger connection.</h1><p>Simple explanations for the decisions that matter.</p>${Object.keys(EXPLAIN).map(explain).join('')}<a class="button" href="#builder">Put it into practice ↗</a></section>`;else if(route==='about')main.innerHTML=`<section class="text-page"><div class="eyebrow">LEARN TO CONNECT</div><h1>Connectivity is<br>a community effort.</h1><p>L2C helps communities and local stakeholders understand, deploy and sustain digital connectivity.</p><p>This lab is for planners, decision-makers, local ISPs, technicians, digital entrepreneurs and connectivity champions. Explore technology-neutral choices and learn why a design could work.</p><div class="panel"><h2>Learn by exploring</h2><p>Our first lab uses transparent educational rules. It offers a starting point for conversations with providers and practitioners.</p><p>L2C Lab is an independent educational prototype, not an official Ericsson product.</p></div><a class="button" href="#labs">Explore the labs ↗</a></section>`;else if(route==='tower')main.innerHTML=towerIntro();else if(route==='tower-play')main.innerHTML=towerLab();else if(route==='cost')main.innerHTML=costIntro();else if(route==='cost-play')main.innerHTML=costGame();else if(route==='cost-results')main.innerHTML=costResults();else main.innerHTML=home();if(route==='tower-play')bindTower();else if(route==='cost-play'||route==='cost-results')bindCost();else bind();if(route==='labs')document.querySelector('#labs').scrollIntoView();else window.scrollTo(0,0);main.focus({preventScroll:true});}
function bind(){main.querySelectorAll('input').forEach(input=>input.addEventListener('change',()=>{const k=input.name;if(k==='priorities'){if(input.checked&&answers.priorities.length>=2){input.checked=false;document.querySelector('#selection-note').textContent='Choose up to two. Deselect a priority to choose another.';return;}answers.priorities=input.checked?[...answers.priorities,input.value]:answers.priorities.filter(v=>v!==input.value);document.querySelector('#selection-note').textContent=`${answers.priorities.length} of 2 priorities selected`;}else answers[k]=input.value;main.querySelectorAll('input').forEach(el=>el.closest('label').classList.toggle('selected',el.checked));main.querySelectorAll('input').forEach(el=>el.closest('label').querySelector('.check').textContent=el.checked?'✓':'○');document.querySelector('#next').disabled=!valid();}));document.querySelector('#next')?.addEventListener('click',()=>{if(!valid())return;if(step<5){step++;render();}else location.hash='results';});document.querySelector('#previous')?.addEventListener('click',()=>{if(step>0){step--;render();}});document.querySelector('#reset')?.addEventListener('click',()=>{answers={priorities:[]};step=0;location.hash='builder';});}
window.addEventListener('hashchange',render);render();

