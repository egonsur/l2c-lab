let towerState={...TOWER_PRESETS[0]},towerPreset=0;
const TOWER_STATUS={good:['Good link geometry','The straight LoS and the inner 60% of the first Fresnel-zone radius clear the effective-Earth terrain and obstacles. This is a geometry check, not proof of a working radio link.'],clearance:['Marginal — clear LoS, limited Fresnel clearance','The straight LoS clears the effective-Earth terrain and obstacles, but the 60% Fresnel clearance target is not met. Seeing the other site is not enough.'],blocked:['Blocked','The effective-Earth terrain or an obstacle meets or crosses the straight LoS. Try raising antennas, shortening the link, or choosing another route.']};
function towerIntro(){return `<section class="text-page tower-intro"><a class="back-link" href="#labs">← All labs</a><div class="eyebrow">LAB 02 · EXPLORE THE INVISIBLE</div><div class="tower-intro-art" aria-hidden="true">⌁ <span>············</span> ⌁</div><h1>Can You See the Tower?</h1><h2>Can these two locations communicate wirelessly?</h2><p>Wireless links can connect locations without digging trenches or extending fiber. But mountains, trees, buildings — and even the curvature of the Earth — can get in the way.</p><p>Build a link and see what happens.</p><a class="button" href="#tower-play">Build a link ↗</a><p class="meta">◷ 2–3 minutes · Beginner / Intermediate</p></section>`;}
function towerSlider(key,label,min,max,unit){return `<div class="tower-control"><label for="tower-${key}">${label}<output id="value-${key}" for="tower-${key}">${towerState[key]} ${unit}</output></label><input id="tower-${key}" data-tower="${key}" type="range" min="${min}" max="${max}" step="1" value="${towerState[key]}" aria-valuetext="${towerState[key]} ${unit}"><div class="range-ends"><span>${min} ${unit}</span><span>${max} ${unit}</span></div></div>`;}
function towerLab(){return `<section class="tower-workspace"><a class="back-link" href="#labs">← All labs</a><div class="eyebrow">LAB 02 · CAN YOU SEE THE TOWER?</div><h1>A clear view is just the beginning.</h1><p class="tower-lead">Choose a scenario. Move an antenna. Discover the space a wireless signal needs.</p><div class="preset-grid" role="group" aria-label="Scenario presets">${TOWER_PRESETS.map((p,i)=>`<button class="preset ${towerPreset===i?'active':''}" data-preset="${i}" aria-pressed="${towerPreset===i}"><small>0${i+1}</small><b>${p.name}</b><span>${p.distance+' km · '+p.frequency+' GHz'}</span></button>`).join('')}</div><div class="tower-layout"><div class="tower-visual"><div class="profile-heading"><div><div class="eyebrow">YOUR RADIO PATH</div><h2>Between two communities</h2></div><span id="profile-distance"></span></div><div id="tower-profile"></div><div class="profile-legend"><span><i class="legend-radio"></i>Straight LoS</span><span><i class="legend-los"></i>Terrain without curvature</span><span><i class="legend-curvature"></i>Earth-curvature correction</span><span><i class="legend-zone"></i>First Fresnel zone</span><span><i class="legend-inner"></i>60% radius target</span><span><i class="legend-ground"></i>Terrain on effective Earth</span></div><p class="note">Synthetic profile · Fixed vertical scale: −40 to 150 m, exaggerated. Axis: height above the endpoint chord. Solid ground = raw terrain elevation + Earth correction. The dashed terrain reference and numeric terrain elevations stay unchanged with distance. LoS always stays straight.</p><div id="tower-result" aria-live="polite" aria-atomic="true"></div></div><aside class="tower-controls" aria-label="Adjust the link"><h2>Make a change</h2><p>Watch the path respond.</p>${towerSlider('distance','Distance',1,50,'km')}${towerSlider('a',towerState.terrain==='urban'?'Site A mast above rooftop':'Site A antenna above ground',5,50,'m')}${towerSlider('b',towerState.terrain==='urban'?'Site B mast above rooftop':'Site B antenna above ground',5,50,'m')}${towerState.terrain==='urban'?towerSlider('roofA','Site A roof above ground',10,40,'m')+towerSlider('roofB','Site B roof above ground',10,40,'m')+towerSlider('building','Intervening building height',10,45,'m'):''}<div class="tower-control"><label for="tower-frequency">Frequency</label><select id="tower-frequency" data-tower="frequency">${TOWER_BANDS.map(f=>`<option value="${f}" ${f===towerState.frequency?'selected':''}>${f} GHz</option>`).join('')}</select><small>A higher frequency narrows the Fresnel zone; it does not automatically improve the link.</small></div><div class="tower-control"><label for="tower-terrain">Terrain / obstruction</label><select id="tower-terrain" data-tower="terrain">${[['flat','Mostly flat'],['hill','Significant central hill'],['trees','Tree line · 16 m tall'],['buildings','Building toward Site B'],['urban','Urban rooftops']].map(([v,l])=>`<option value="${v}" ${v===towerState.terrain?'selected':''}>${l}</option>`).join('')}</select></div><label class="earth-toggle"><input id="tower-curvature" data-tower="curvature" type="checkbox" ${towerState.curvature?'checked':''}> Include Earth curvature</label><p class="note">Earth curvature uses an effective radius of 4/3 × Earth’s radius (k = 4/3).</p><button id="tower-reset" class="secondary">Reset this scenario ↺</button></aside></div><section class="panel"><h2>Being able to see the other site is important — but it is not always enough.</h2><div id="tower-lessons"></div><div class="trade-grid">${explain('los')}${explain('terrain')}<details class="explanation"><summary>Why does frequency change the shape?</summary><p>Lower frequencies have a larger first Fresnel zone for the same distance. Higher frequencies need a narrower clearance envelope, but have different propagation, equipment and weather considerations.</p><p>Rain and atmospheric absorption can be important, particularly at higher frequencies. This lab does not calculate received power or availability.</p></details><details class="explanation"><summary>Why does Earth curvature matter?</summary><p>The Earth curves toward the straight antenna-to-antenna chord. Its midpoint correction grows with distance squared. We add this correction only when positioning ground and obstacles in the effective-Earth view. The LoS and the center of the Fresnel zone stay on the same straight line.</p><p>We use k = 4/3 to approximate typical atmospheric refraction with an effective Earth radius. The solid terrain profile includes this Earth correction; the dashed reference shows raw terrain elevations without it. A hill’s actual height does not increase when distance changes. Actual refraction varies; this assumption does not guarantee year-round clearance.</p></details></div><details class="explanation"><summary>See the model and its limits</summary><p>First Fresnel radius: r₁ = √(λ d₁ d₂ / D), with λ = c / f and D = d₁ + d₂. Effective Earth bulge: h = d₁ d₂ / (2 k R), R = 6,371,000 m. Antenna phase centers equal endpoint terrain elevation plus rooftop height, if present, plus mast height. Terrain is a normalized horizontal shape with fixed elevations. Only horizontal coordinates change with link distance. The tree line occupies 42–58% of the path, modeled conservatively as continuous vegetation 16 m above local ground. Icons match that height approximately; canopy widths and visual gaps do not change obstruction calculations.</p><p>We sample 1,001 points. “Blocked” means minimum straight-LoS clearance over curvature-positioned obstacles ≤ 0. Otherwise a minimum margin below 0 above the 60% Fresnel-radius envelope means Marginal. Remaining cases have good geometry under the selected assumptions. The Fresnel envelope uses the small-angle path-profile approximation.</p><p>The 60% radius target is a simplified educational screening rule, not a complete design criterion. It is not “60% of the area.” No real elevation data, diffraction loss, link budget, interference, rain fade or availability calculation is included.</p><p>Technical background: <a href="https://www.itu.int/dms_pubrec/itu-r/rec/p/R-REC-P.530-16-201507-S%21%21PDF-E.pdf" target="_blank" rel="noopener">ITU-R P.530 propagation guidance ↗</a>.</p></details><p class="disclaimer">Educational visualization, not professional RF planning. Frequency bands have different licensing, permitted power and coordination rules depending on country and use. Confirm these with the relevant regulator. A real deployment needs a site survey, terrain and clutter data, link-budget and availability analysis, and checks of equipment, spectrum and power.</p></section><div class="tower-end"><a href="#tower" class="back-link">← Lab introduction</a><a class="button" href="#builder">Explore connectivity options ↗</a></div></section>`;}
// Shared fixed chart frame: independent of distance, frequency and scenario.
const PROFILE_SCALE={min:-40,max:150,bottom:340,pixelsPerMetre:1.5};
// Physical height determines Y extents. Decorative canopy width never enters evaluatePath.
function treeLineSVG(s,X,Y,canopyWidth=16){
 const positions=Array.from({length:9},(_,i)=>VEGETATION.start+(VEGETATION.end-VEGETATION.start)*i/8);
 return '<g data-layer="vegetation"><title>Tree line: 16 m above ground, from 42% to 58% of the path. Canopy widths are illustrative.</title>'+positions.map(t=>{
  const ground=terrainHeight(t,s.terrain)+(s.curvature?earthCorrection(t*s.distance*1000,(1-t)*s.distance*1000):0),baseY=Y(ground),topY=Y(ground+VEGETATION.height),h=baseY-topY,x=X(t),w=canopyWidth/2;
  return `<g data-tree-height="${VEGETATION.height}" data-tree-base="${baseY}" data-tree-top="${topY}" transform="translate(${x} ${topY})"><path d="M0 ${h} L0 ${h*.46} M0 ${h*.72} L${-w*.4} ${h*.51} M0 ${h*.66} L${w*.4} ${h*.43}" fill="none" stroke="#79583d" stroke-width="2"/><path d="M0 0 C${w*.6} 0 ${w*.75} ${h*.12} ${w*.65} ${h*.24} C${w*1.2} ${h*.24} ${w*1.2} ${h*.56} ${w*.65} ${h*.63} C${w*.4} ${h*.78} ${-w*.4} ${h*.78} ${-w*.65} ${h*.63} C${-w*1.2} ${h*.56} ${-w*1.2} ${h*.24} ${-w*.65} ${h*.24} C${-w*.75} ${h*.12} ${-w*.6} 0 0 0 Z" fill="#5b8955" stroke="#365e3d" stroke-width="1"/></g>`;
 }).join('')+`<text x="${X(.5)}" y="${Y(terrainHeight(.5,s.terrain)+(s.curvature?earthCorrection(s.distance*500,s.distance*500):0)+VEGETATION.height)-12}" text-anchor="middle" class="site-label">Tree line · ${VEGETATION.height} m tall</text></g>`;
}
function buildingsSVG(s,r,X,Y){
 return '<g data-layer="buildings">'+buildingProfiles(s).map((b,i)=>{
  const points=r.samples.filter(p=>p.t>=b.start&&p.t<=b.end);
  const outline=points.map(p=>X(p.t)+','+Y(p.displayGround+b.height)).concat([...points].reverse().map(p=>X(p.t)+','+Y(p.displayGround))).join(' ');
  const mid=points[Math.floor(points.length/2)],width=X(b.end)-X(b.start);
  const windows=Array.from({length:Math.max(1,Math.floor(b.height/7))},(_,row)=>[.25,.5,.75].map(frac=>{
   const t=b.start+(b.end-b.start)*frac,p=r.samples[Math.round(t*1000)];
   return `<rect x="${X(t)-2}" y="${Y(p.displayGround+4+row*7)}" width="4" height="3" fill="#eef5f5"/>`;
  }).join('')).join('');
  return `<g data-building-height="${b.height}"><title>${b.name}: ${b.height} m above ground</title><polygon points="${outline}" fill="${i===1?'#a4afb9':'#bdc9d0'}" stroke="#677b88" stroke-width="1.5"/>${windows}<text x="${X(mid.t)}" y="${Y(mid.displayGround)+14}" text-anchor="middle" class="axis-label">${b.height} m</text></g>`;
 }).join('')+'</g>';
}
function profileSVG(s,r){
 const X=t=>70+860*t,Y=h=>PROFILE_SCALE.bottom-(h-PROFILE_SCALE.min)*PROFILE_SCALE.pixelsPerMetre;
 const line=(points,fn)=>points.map(p=>X(p.t).toFixed(2)+','+Y(fn(p)).toFixed(2)).join(' ');
 const zone=f=>line(r.samples,p=>p.los+f*p.fresnel)+' '+line([...r.samples].reverse(),p=>p.los-f*p.fresnel);
 const correction=line(r.samples,p=>p.displayGround)+' '+line([...r.samples].reverse(),p=>p.ground);
 const bad=r.samples.filter((p,i)=>i%10===0&&p.margin<0);
 return `<svg viewBox="0 0 1000 410" role="img" aria-labelledby="path-title path-desc"><title id="path-title">Wireless path: ${TOWER_STATUS[r.status][0]}</title><desc id="path-desc">Terrain peak ${r.peak.ground.toFixed(1)} metres; Earth correction at that point ${r.peak.bulge.toFixed(1)} metres. Straight LoS joins phase centers ${r.antennaA} and ${r.antennaB} metres above the endpoint datum. Link ${s.distance} kilometres at ${s.frequency} GHz. Minimum LoS clearance ${r.minLOS.toFixed(1)} metres; minimum 60 percent Fresnel margin ${r.worst.margin.toFixed(1)} metres. Fixed vertical range minus 40 to 150 metres.</desc><rect width="1000" height="350" rx="10" fill="#f2f7f7"/>${[-25,0,25,50,75,100,125,150].map(h=>`<line x1="70" x2="930" y1="${Y(h)}" y2="${Y(h)}" stroke="#dce7e4"/><text data-elevation="${h}" x="55" y="${Y(h)+4}" text-anchor="end" class="axis-label">${h} m</text>`).join('')}<polygon data-layer="terrain-fill" points="70,340 ${line(r.samples,p=>p.displayGround)} 930,340" fill="#bdcdb3"/>${s.curvature?`<polygon data-layer="curvature" points="${correction}" fill="#cba66a" fill-opacity=".38"/>`:''}<polyline data-layer="terrain" points="${line(r.samples,p=>p.ground)}" fill="none" stroke="#8d713d" stroke-dasharray="4 4" stroke-width="1.5"/><polyline data-layer="effective-terrain" points="${line(r.samples,p=>p.displayGround)}" fill="none" stroke="#627f59" stroke-width="2"/>${buildingsSVG(s,r,X,Y)}${s.terrain==='trees'?treeLineSVG(s,X,Y):''}<polygon data-layer="fresnel" points="${zone(1)}" fill="#72b5d5" fill-opacity=".24" stroke="#5e97b1" stroke-width="1"/><polygon data-layer="fresnel60" points="${zone(.6)}" fill="#72b5d5" fill-opacity=".2" stroke="#447c9c" stroke-dasharray="5 4"/>${bad.map(p=>`<line x1="${X(p.t)}" x2="${X(p.t)}" y1="${Y(p.displayObstacle)}" y2="${Y(p.los-.6*p.fresnel)}" stroke="#be704b" stroke-width="3" opacity=".65"/>`).join('')}<line data-layer="radio" x1="70" x2="930" y1="${Y(r.antennaA)}" y2="${Y(r.antennaB)}" stroke="#185d4e" stroke-width="2.5"/>${[[70,r.mountA,s.a,'A'],[930,r.mountB,s.b,'B']].map(([x,g,h,l])=>`<path d="M${x-7} ${Y(g)} L${x} ${Y(g+h)} L${x+7} ${Y(g)} M${x-5} ${Y(g+h*.3)} L${x+4} ${Y(g+h*.6)}" fill="none" stroke="#294e45" stroke-width="2.5"/><circle data-phase-center="${l}" cx="${x}" cy="${Y(g+h)}" r="5" fill="#185d4e" stroke="white" stroke-width="2"/><text x="${x}" y="${Y(g+h)-14}" text-anchor="${l==='A'?'start':'end'}" class="site-label">${l} · ${h} m ${s.terrain==='urban'?'above roof':'above ground'}</text>`).join('')}${r.status!=='good'?`<circle cx="${X(r.worst.t)}" cy="${Y(r.worst.displayObstacle)}" r="6" fill="#b05639" stroke="white" stroke-width="2"/>`:''}${s.terrain==='hill'?`<text data-layer="peak-label" x="${X(r.peak.t)}" y="${Y(r.peak.displayGround)-12}" text-anchor="middle" class="site-label">Hill: ${r.peak.ground.toFixed(1)} m + Earth: ${r.peak.bulge.toFixed(1)} m</text>`:''}<text x="70" y="370" class="site-label">Site A — ${s.terrain==='urban'?'Network rooftop':'Network Site'}</text><text x="930" y="370" text-anchor="end" class="site-label">Site B — ${s.terrain==='urban'?'Community rooftop':'Community Site'}</text>${[0,.25,.5,.75,1].map(t=>`<text data-distance="${s.distance*t}" x="${X(t)}" y="397" text-anchor="middle" class="axis-label">${+(s.distance*t).toFixed(2)} km</text>`).join('')}</svg>`;
}
function updateTower(){
 const s=towerState,r=evaluatePath(s);
 document.querySelector('#tower-profile').innerHTML=profileSVG(s,r);
 const modified=Object.keys(towerState).some(key=>towerState[key]!==TOWER_PRESETS[towerPreset][key]); document.querySelector('#profile-distance').textContent=(modified?'Modified · ':'')+s.distance+' km · '+s.frequency+' GHz';
 document.querySelector('#tower-result').innerHTML=`<div class="path-status status-${r.status}"><h3>${r.status==='good'?'✓':r.status==='blocked'?'⊘':'△'} ${TOWER_STATUS[r.status][0]}</h3><p>${TOWER_STATUS[r.status][1]}</p>${!s.curvature?'<p><b>Curvature is off:</b> this is an artificial flat-Earth comparison.</p>':''}<p id="terrain-peak"><b>Terrain peak: ${r.peak.ground.toFixed(1)} m</b> at ${(r.peak.x/1000).toFixed(1)} km. Distance changes its position, not its elevation.</p>${s.terrain==='urban'?`<p><b>Phase centers:</b> A ${r.antennaA} m (${s.roofA} m roof + ${s.a} m mast); B ${r.antennaB} m (${s.roofB} m roof + ${s.b} m mast). Intervening building: ${s.building} m above ground.</p>`:''}${s.terrain==='trees'?'<p>Tree line: <b>16 m above local ground</b> across 42–58% of the path. Canopy widths and gaps are illustrative.</p>':''}<div class="path-metrics"><div><b>${r.minLOS.toFixed(1)} m</b><small>Minimum LoS clearance</small></div><div><b>${r.worst.margin.toFixed(1)} m</b><small>Minimum 60% zone margin</small></div><div><b>${r.midRadius.toFixed(1)} m</b><small>Midpoint Fresnel radius</small></div><div><b>${r.midBulge.toFixed(1)} m</b><small>Midpoint curvature correction</small></div></div></div>`;
 document.querySelector('#tower-lessons').innerHTML=`<div class="tower-takeaways">${s.terrain==='urban'?'<article><h3>Rooftop height is part of the link.</h3><p>The phase center is roof height plus mast height. Raise a mast or lower the intervening building to explore Marginal, Good and Blocked outcomes. Rooftop access, structural suitability and nearby buildings also need a survey.</p></article>':''}<article><h3>${r.status==='good'?'Keep exploring.':'Try a change.'}</h3><p>${r.status==='blocked'?'Raise both antennas or switch to mostly flat terrain. Changing frequency cannot remove a direct-path obstruction.':r.status==='clearance'?'Raise the antennas until the clearance target is met. Notice that the visible path was already clear.':'Lower Site B or lengthen the link. Notice when the Fresnel zone touches terrain before the direct path does.'}</p></article><article><h3>Distance has two effects.</h3><p>A longer link widens the Fresnel zone and increases Earth bulge. ${s.distance>=20?'Toggle curvature: the ground view changes, but LoS stays straight and the Fresnel zone stays centered on it. Raw terrain elevations remain fixed.':'Try the long-distance preset to make Earth curvature easier to see.'}</p></article><article><h3>Geometry is one part.</h3><p>${s.frequency>=18?'A narrow zone at this frequency does not account for rain, atmospheric attenuation or available fade margin.':'A clear corridor still needs enough received signal, suitable equipment and an interference assessment.'}</p></article></div>`;
}
function bindTower(){
 document.querySelectorAll('[data-preset]').forEach(b=>b.addEventListener('click',()=>{towerPreset=Number(b.dataset.preset);towerState={...TOWER_PRESETS[towerPreset]};main.innerHTML=towerLab();bindTower();document.querySelector('[data-preset="'+towerPreset+'"]').focus();}));
 document.querySelectorAll('[data-tower]').forEach(el=>el.addEventListener('input',()=>{
  const wasUrban=towerState.terrain==='urban'; const key=el.dataset.tower;towerState[key]=key==='curvature'?el.checked:key==='terrain'?el.value:Number(el.value);
  if(key==='terrain'&&(wasUrban||towerState.terrain==='urban')){if(towerState.terrain==='urban'){for(const [k,v] of Object.entries(URBAN_DEFAULTS))towerState[k]??=v;}main.innerHTML=towerLab();bindTower();document.querySelector('#tower-terrain').focus();return;}
  const output=document.querySelector('#value-'+key);if(output){const unit=key==='distance'?'km':'m';output.textContent=el.value+' '+unit;el.setAttribute('aria-valuetext',el.value+' '+unit);}
  document.querySelectorAll('[data-preset]').forEach(b=>{const active=Number(b.dataset.preset)===towerPreset;b.classList.toggle('active',active);b.setAttribute('aria-pressed',String(active));});
  updateTower();
 }));
 document.querySelector('#tower-reset').addEventListener('click',()=>{towerState={...TOWER_PRESETS[towerPreset]};main.innerHTML=towerLab();bindTower();document.querySelector('#tower-reset').focus();});
 updateTower();
}




