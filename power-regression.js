(function(){const out=[];function ok(name,v){if(!v)throw Error(name);out.push('✓ '+name)};ok('100 W × 10 h = 1 kWh',powerEnergy(100,10)===1);ok('100 W continuous = 2.4 kWh/day',continuousDailyEnergy(100)===2.4);ok('500 W × 5 h = 2.5 kWh',powerEnergy(500,5)===2.5);const school=POWER.scenarios.school;ok('PoE switch and AP load are not double counted',networkLoad(school)===165);ok('satellite is default',school.accessTech==='leo');const originalTech=school.accessTech;school.accessTech='fwa';ok('FWA reduces local load by 50 W',networkLoad(school)===115);school.accessTech='p2p';ok('P2P counts one local radio',networkLoad(school)===110);school.accessTech='fiber';ok('fiber counts ONT/CPE only',networkLoad(school)===100);school.accessTech=originalTech;ok('solar is zero at night',solarAt(school,2)===0);ok('cloudy solar is lower than sunny',solarAt(school,12,'Cloudy')<solarAt(school,12,'Sunny'));const sunny=simulatePower(school,{weather:'Sunny'});ok('battery state remains bounded',sunny.steps.every(x=>x.batteryPct>=0&&x.batteryPct<=1));ok('scheduled load uses less energy than 24×7',dailyDemand(school)<dailyDemand({...school,mode:'24x7'}));ok('adding devices increases demand',dailyDemand({...school,devices:{...school.devices,count:school.devices.count+1}})>dailyDemand(school));const outage=simulatePower(POWER.scenarios.health,{weather:'Sunny'});ok('health grid outage still uses stored battery',outage.steps.find(x=>x.h===18).grid===0);ok('small off-grid system can go offline',simulatePower({...school,solar:250,battery:1},{weather:'Cloudy'}).availability<1);document.getElementById('out').textContent=out.join('\n')+'\nAll Lab 5 checks passed.'})().catch(e=>{document.getElementById('out').textContent='FAIL: '+e.message;document.body.dataset.failed='true'});
